// Perfrom CRUD
// Array.phototype.push()
// the push() method adds one or more elements to the end of array
// and returns the new lenghth of array.

const animal=['pigs','goat','sheep'];
//animal.push('chiken');
console.log(animal);  //[ 'pigs', 'goat', 'sheep', 'chiken' ]
//console.log(count); // it print 4. that is length of array.

animal.push('beef','peacock');
console.log(animal);


